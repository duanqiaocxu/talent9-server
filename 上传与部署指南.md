# 上传到 GitHub 与部署指南

> 本指南给你**本机一键执行**的命令。沙箱环境无 `git`/`gh`，所以「推代码」这步由你在自己电脑上跑；推之前的全部安全化、部署化改造我已经做完。

---

## 0. 前置准备
1. 本机安装 Git：<https://git-scm.com/downloads>（默认勾选「Git Bash Here」即可）。
2. 一个 GitHub 账号（<https://github.com>）。
3. 若要走自动部署：一个 Render 账号（<https://render.com>，用 GitHub 登录，免费）。
4. 确认本目录已含：`server.js`、`public/app.html`、`admin.html`、`keys/public.pem`、`package.json`、`render.yaml`、`.gitignore`、`.env.example`。

---

## 1. 本机初始化仓库并首次提交（在 Git Bash 里执行）

打开 `talent9-server` 目录（右键「Git Bash Here」，或 `cd` 进去），依次执行：

```bash
# 进入项目目录（按你实际路径）
cd "/c/Users/jsqiang/Desktop/talent9-server"

# 初始化
git init
git branch -M main

# 加入所有文件（.gitignore 会自动排除私钥/.env/licenses.json 等）
git add .

# ⚠️ 安全复核：先看一下哪些文件会被提交，确认没有密钥泄露
git status
```

**安全复核（必须做）**：`git status` 的输出里**绝不能出现**以下文件——
- `keys/private.pem`
- `.env`
- `licenses.json`
- `crash.txt` / `ready.txt` / `*result.txt`

如果它们出现了，说明 `.gitignore` 没生效，**立刻停手**检查 `.gitignore`，不要继续 `commit`。

确认无误后提交：

```bash
git commit -m "人才盘点九宫格 · 付费可视化工作台（Ed25519 授权 + 微信/Stripe 支付 + 钉钉/飞书免登）"
```

---

## 2. 在 GitHub 新建空仓库
1. 打开 <https://github.com/new>
2. 仓库名填 `talent9-grid`（或你喜欢的名字），**可见性选 Private（私有，推荐）**
3. **不要**勾选「Add a README file」「.gitignore」「license」（保持空仓库）
4. 点「Create repository」，页面会给出仓库地址，形如：
   `https://github.com/<你的用户名>/talent9-grid.git`

---

## 3. 推送到 GitHub

把上一步的地址替换进下面命令（用你自己的用户名）：

```bash
git remote add origin https://github.com/<你的用户名>/talent9-grid.git
git push -u origin main
```

首次推送会弹窗要你登录 GitHub（或用 Personal Access Token）。推送完成后，去 GitHub 仓库页面确认：
- 文件列表里有 `server.js`、`public/app.html`、`keys/public.pem` 等 ✅
- **没有** `keys/private.pem`、`.env`、`licenses.json` ✅（被 `.gitignore` 挡住了）

> 若将来要改代码再推：`git add . && git commit -m "说明" && git push`

---

## 4. 一键部署到 Render（推荐）

1. 登录 <https://render.com>（用 GitHub 授权）。
2. 点「New」→「Blueprint」，选择刚推上去的 `talent9-grid` 仓库。
3. Render 会自动读取仓库根目录的 `render.yaml`，显示名为 `talent9-grid` 的 Web Service。
4. 在环境变量里**逐项填写**（私钥/口令尤其重要）：
   - `LICENSE_PRIVATE_KEY_PEM`：**把本地 `keys/private.pem` 的整段内容粘进来**（包含 `-----BEGIN PRIVATE KEY-----` 和 `-----END PRIVATE KEY-----` 两行，连同中间内容，一个字都别漏）。
   - `ADMIN_TOKEN`：设一个你自己记得的强口令（管理后台 `/admin` 用）。
   - `PUBLIC_BASE_URL`：填 Render 给你的地址，形如 `https://talent9-grid.onrender.com`（部署完成后在 Dashboard 能看到，先留空也可，建好后再补）。
   - 其余 `STRIPE_*` / `WECHAT_*` / `DINGTALK_*` / `FEISHU_*` **暂时留空**就是 DEMO 模式（可完整跑通下单→解锁，但不收真实钱）。
5. 点「Deploy」。约 1–2 分钟构建完成，得到公网地址。
6. 拿到地址后，回到 Render 环境变量把 `PUBLIC_BASE_URL` 补成 `https://talent9-grid.onrender.com`，Save 并等重启。

### 验证上线
浏览器打开 `https://talent9-grid.onrender.com/api/config`，应返回类似：
```json
{ "demo": true, "providers": { "stripe": false, "wechat": false, "dingtalk": false, "feishu": false } }
```
再打开 `https://talent9-grid.onrender.com/` 就是九宫格工作台。

---

## 5. 其他部署方式

### 容器 / Railway
仓库已含 `Dockerfile` + `Procfile`：
```bash
docker build -t talent9 .
docker run -p 8787:8787 \
  -e LICENSE_PRIVATE_KEY_PEM="$(cat keys/private.pem)" \
  -e ADMIN_TOKEN="你的口令" \
  -e PUBLIC_BASE_URL="https://你的域名" \
  talent9
```
Railway 直接连 GitHub 仓库、识别 `Procfile` 即可。

### 自有云主机 + HTTPS
- 把目录拷到服务器，`node server.js`（或 `npm start`）常驻（建议用 `pm2` 或 systemd）。
- `PUBLIC_BASE_URL` 填你的域名；前面用 Nginx/Caddy 反代并配 HTTPS（Let's Encrypt 免费证书）。
- 服务端已默认监听 `0.0.0.0`，无需改代码。

---

## 6. 安全复核清单（上线前逐项打钩）

- [ ] GitHub 仓库里搜不到 `keys/private.pem`（私有仓库也建议确认）
- [ ] GitHub 仓库里搜不到 `.env`、`licenses.json`
- [ ] 部署平台 Secret 里 `LICENSE_PRIVATE_KEY_PEM` 已填（私钥整段）
- [ ] `ADMIN_TOKEN` 已设（否则后台接口仅 DEMO 放行，有风险）
- [ ] `PUBLIC_BASE_URL` 已填公网 https 地址
- [ ] 仅当你要收真实钱时，才填 `STRIPE_*` / `WECHAT_*`；不填就是安全的 DEMO 模式

---

## 7. 常见问题

**Q：私有仓库还用得着 `.gitignore` 排除私钥吗？**
用。防止误推、防止协作者 clone 后泄露、也防止哪天改成公开库时忘了清密钥。密钥永远走平台 Secret 注入。

**Q：钥匙（private.pem）丢了怎么办？**
用 `gen_keys.js` 重新生成（`node gen_keys.js`），把新 `private.pem` 全文更新到平台 Secret，并把新 `public.pem` 提交进仓库、同步更新客户端 HTML 里 `SERVER_PUBLIC_URL` 对应的嵌入公钥（当前为 `MCowBQYDK2VwAyEA+DHZyyRuzbNyFByet0dBLl5Wl2vBev4UmRSbfY8m/kI=`）。旧令牌会失效，需重新签发。

**Q：免费层 Render 偶尔打不开？**
免费实例约 15 分钟无流量会休眠，首次访问有数秒冷启动。绑定信用卡或升级付费层可消除休眠。

**Q：想先只上传、暂不部署？**
做到第 3 步 `git push` 即可，第 4 步随时再做。
